//
//  FetchWeatherConditionAPI.swift
//  YumemiWorks
//
//  Created by user on 2023/09/05.
//

/*
 import UIKit
 import YumemiWeather
 
 class FetchWeatherConditionAPI {
 
 func fetchWeatherCondition(delegate: weatherConditionDelegate) {
 let area = "tokyo" // 現状では固定値で仮入れ
 do {
 let weatherString = try YumemiWeather.fetchWeatherCondition(at: area)
 let condition = WeatherCondition(weatherString: weatherString)
 delegate.didChangedWeather(weatherCondition: condition)
 } catch {
 print("Exception Occured. area = \(area), error = \(error)")
 delegate.failedToFetchWeather(error: error)
 }
 
 }
 
 }
 */
